name: streamsd
description: Catálogo M3U pessoal com seleção de entradas SD.
publish_to: none
version: 1.1.1+3

environment:
  sdk: '>=3.5.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  file_picker: ^13.1.0
  http: ^1.6.0
  path_provider: ^2.1.6
  shared_preferences: ^2.5.5
  video_player: ^2.14.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0

flutter:
  uses-material-design: true
